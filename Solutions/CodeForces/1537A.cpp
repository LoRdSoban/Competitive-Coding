#include <bits/stdc++.h>

#define forn(n) for(int i=0;i<n;i++)
#define IOS ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

using namespace std;


int main()
{
    IOS;
    int T;

    int n, sum, a,ans;

    cin >> T;
    while (T--)
    {
        sum =0;
        cin >> n;
        forn(i)
        {
            cin >> a;
            sum += a;
        }

        
    }

    return 0;
}


`